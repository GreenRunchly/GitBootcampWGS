<div class="loadingScreen">
    <style>
        .loadingScreen{
            top: 0;
            left: 0;
            position: fixed;
            width: 100vw;
            height: 100vh;
            background-color: rgba(255, 255, 255, 1);
            z-index: 999999;
            opacity: 1;
            transition: 1s;
        }
        .loadingScreen.done{
            background-color: rgba(255, 255, 255, 0);
        }
        .loadingScreen > .bar{
            position: relative;
            width: 100vw;
            height: 0.5rem;
            background-color: blueviolet;
            animation: loadingBar 1s infinite cubic-bezier(.34,.84,.67,.38);
            opacity: 1;
            transition: 1s;
        }
        .loadingScreen.done .bar{
            opacity: 0;
        }
        @keyframes loadingBar {
            0%{
                left: 0vw;
                width: 0vw;
            }
            25%{
                left: 0vw;
                width: 75vw;
            }
            50%{
                left: 25vw;
                width: 100vw;
            }
            75%{
                left: 100vw;
                width: 0vw;
            }
            100%{
                left: 0vw;
                width: 0vw;
            }
        }
    </style>
    <div class="bar"></div>
</div>